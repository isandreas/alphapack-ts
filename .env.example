BOT_TOKEN=
REDIS_URL=redis://127.0.0.1:6379
DEFAULT_LOCALE=id
NODE_ENV=production
LOG_LEVEL=info

# Custom name for the bot used in welcome, goodbye, rules, and guide templates.
# Defaults to the bot's Telegram first_name if omitted.
BOT_CUSTOM_NAME="Alpha Pack"